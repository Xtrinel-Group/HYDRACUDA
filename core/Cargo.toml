[package]
name = "hydracuda-core"
description = "Offline policy decision engine for AI agent tool calls"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
fancy-regex.workspace = true
indexmap.workspace = true
yaml-rust2.workspace = true

[dev-dependencies]
# Only the differential harness needs JSON: the expectations recorded from the
# Python engine are a committed JSON file.
serde_json = "1"
indexmap.workspace = true
